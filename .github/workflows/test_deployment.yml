- name: Test SSH Connection
  uses: appleboy/ssh-action@v0.1.16
  with:
    host: ${{ secrets.VPS_HOST }}
    username: ${{ secrets.VPS_USER }}
    key: ${{ secrets.VPS_SSH_KEY }}
    script: |
      echo "Connecté au VPS en tant que $USER"
      whoami
      ls -la